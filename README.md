# Selenium Base Examples

Messing around with Selenium base cos I haven't done Python in ages.

### Installing

```
pip install -r requirements.txt
```

### Example Useage

```
pytest first_test.py --browser=chrome
```
